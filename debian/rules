#!/usr/bin/make -f

export LDFLAGS=
DEB_HOST_GNU_TYPE ?= $(shell dpkg-architecture -qDEB_HOST_GNU_TYPE)

%:
	dh $@ --builddirectory=.obj-$(DEB_HOST_GNU_TYPE)

override_dh_auto_configure:
	./tools/configure.sh -l qtif427:nsh
override_dh_auto_build:
	make

